Vendored from npm `three-bvh-csg@0.0.18` (MIT).

Bundled ESM (`build/index.module.js`) that imports `three` and `three-mesh-bvh`.
Loaded on first CSG section commit via import map alias `three-bvh-csg`.
Do not concatenate into application JS.
